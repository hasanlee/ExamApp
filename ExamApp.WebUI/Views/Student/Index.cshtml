@model List<ExamApp.EntityLayer.DTOs.Students.StudentDTO>
<h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light">Student /</span> Student List</h4>
<div class="card">
    <div class="d-flex justify-content-between px-3">
        <h5 class="card-header">Students</h5>
        <div class="d-flex align-items-center">
            <a class="btn btn-outline-success text-success" asp-controller="Student" asp-action="Add">
                <i class="tf-icons bx bx-plus"></i> Add
            </a>
        </div>
    </div>

    <div class="table-responsive text-nowrap">
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Student No</th>
                    <th>Name</th>
                    <th>Class No</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody class="table-border-bottom-0">
                @foreach (var item in Model)
                {
                    <tr>
                        <td><i class="fab fa-angular fa-lg text-danger me-3"></i> <strong>@item.Id</strong></td>
                        <td>@item.StudentNo</td>
                        <td>@item.Name @item.Surname</td>
                        <td>@item.ClassNo</td>
                        <td>
                            <a asp-controller="Student" asp-action="Update" asp-route-studentId="@item.Id"><i class="bx bx-edit-alt me-1"></i></a>
                            <a asp-controller="Student" asp-action="Delete" asp-route-studentId="@item.Id"><i class="bx bx-trash me-1  text-danger"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>